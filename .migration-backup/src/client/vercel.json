    {
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/big-bucks-innovation/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ],
    "rewrites": [
      { "source": "/(.*)", "destination": "/" }
    ]
}